<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/category_selection_gridview_bg"
   
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/search_gridview_image"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:scaleType="centerInside" 
        android:src="@drawable/no_image"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/search_gridview_text"
        android:layout_below="@id/search_gridview_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="图片"
        android:textColor="#ff0000"
        android:layout_alignLeft="@id/search_gridview_image"
         />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
         android:layout_below="@id/search_gridview_image"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="10dp"
        android:layout_marginRight="10dp"
        android:scaleType="centerInside"
        android:src="@drawable/android_product_reduce" />

</RelativeLayout>